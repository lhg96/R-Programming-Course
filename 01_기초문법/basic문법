x = 10  
#x 변수 초기화  
rm(x) 
#전체 메모리 삭제
rm(list = ls())
ls
seq(1,10,by=2)

#ctrl+enter 라인 실행 
m5 = data.frame(id=c("a","b","c","d"),
           score=c(90,80,70,60),
           gender=factor(c("M","F","M","F"))
           )
nrow(m5) #row 개수

#데이터 보기  
View(df)  
